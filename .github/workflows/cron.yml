name: keep server alive

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: ping server
        run: |
          curl https://cbt-platform-api.onrender.com/health \
          --fail \
          --silent \
          --show-error
